[package]
name = "mtbinloader2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Nenhuma dependência externa necessária para a lógica básica de I/O

[profile.githubci]
inherits = "release"
strip = true
lto = true
codegen-units = 1
debug = false
panic = "abort"
